eureka:
  client:
    fetch-registry: 'true'
    register-with-eureka: 'true'
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
spring:
  application:
    name: department-service
server:
  port: '8081'

management:
  tracing:
    sampling:
      probability: '1.0'

